spring:
  application:
    name: tradingbackend

  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI}

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

