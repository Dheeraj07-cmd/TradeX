spring:
  application:
    name: tradingbackend

  data:
    mongodb:
      uri: ${MONGO_URI}
      auto-index-creation: true
#   redis
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password:

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

brevo:
  api:
    key: ${BREVO_API_KEY}
  sender:
    email: ${BREVO_SENDER_EMAIL}

